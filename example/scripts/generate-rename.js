#!/usr/bin/env node

const path = require('node:path');
const fs = require('node:fs');

const project_root = path.resolve(__dirname, '..');

const file = path.join(project_root, 'src', 'rename.uikit.ts');
console.log(`📝 Generate the rename file: ${file}`);
const content = `// This file is automatically generated. The user needs to manually fill in the necessary parameters.\n
export * from 'react-native-chat-uikit';
`;
fs.writeFileSync(file, content, 'utf-8');

const file2 = path.join(project_root, 'src', 'rename.callkit.ts');
console.log(`📝 Generate the rename file: ${file}`);
const content2 = `// This file is automatically generated. The user needs to manually fill in the necessary parameters.\n
export * from 'react-native-chat-callkit';
`;
fs.writeFileSync(file2, content2, 'utf-8');
